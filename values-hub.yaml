---
clusterGroup:
  name: hub
  isHubCluster: true
  namespaces:
    vault:
    external-secrets-operator:
      operatorGroup: true
      targetNamespaces: []
    external-secrets:
    openshift-sscsi:
    openshift-sscsi-vault-demo:
    ansible-automation-platform:
    aap-config:
  subscriptions:
    aap-operator:
      name: ansible-automation-platform-operator
      namespace: ansible-automation-platform
      channel: stable-2.6
    openshift-sscsi:
      name: secrets-store-csi-driver-operator
      namesapce: openshift-cluster-csi-drivers
      channel: stable
  applications:
    aap:
      name: ansible-automation-platform
      namespace: ansible-automation-platform
      chart: ansible-automation-platform-instance
      chartVersion: 0.1.*
    aap-config:
      name: aap-config
      namespace: aap-config
      #chart: aap-config
      #chartVersion: 0.2.*
      repoURL: https://github.com/mhjacks/aap-config-chart.git
      chartVersion: optionalize_secrets
      path: "."
      extraValueFiles:
        - $patternref/overrides/values-aap-config-aeg.yaml
    eso:
      name: openshift-external-secrets-operator
      namespace: external-secrets-operator
      channel: stable-v1
    sscsi:
      name: openshift-sscsi
      namespace: openshift-sscsi
      repoURL: https://github.com/mhjacks/openshift-sscsi-vault-chart.git
      chartVersion: main
      path: "."
    vault:
      name: vault
      namespace: vault
      chart: hashicorp-vault
      chartVersion: 0.1.*
      overrides:
        - name: vault.csi.enabled
          value: true
          forceString: true
  imperative:
    image: quay.io/martjack/vp-utility-container-test:latest
  managedClusterGroups: {}
